YAML format checklist (the answer is intended to be YAML):
- Output must be YAML text only (no explanation, no Markdown).
- Must be parseable YAML (avoid common pitfalls):
  - Consistent indentation; no tab indentation.
  - Correct use of quotes for strings that need it.
  - If multiple documents (---) are not requested, treat them as incorrect.
- Must match the task requirement (expected keys/structure/types).
