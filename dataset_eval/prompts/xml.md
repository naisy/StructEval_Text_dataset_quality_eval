XML format checklist (the answer is intended to be XML):
- Output must be XML text only (no explanation, no Markdown).
- Must be well-formed XML:
  - Exactly one root element.
  - All tags properly nested and closed.
  - Attribute quotes are valid; entities are valid.
- Must match the task requirement (expected tags/structure/content).
