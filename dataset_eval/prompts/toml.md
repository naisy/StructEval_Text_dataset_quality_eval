TOML format checklist (the answer is intended to be TOML):
- Output must be TOML text only (no explanation, no Markdown).
- Must be parseable TOML:
  - Proper key/value syntax, quoting, arrays, and tables.
  - No duplicate keys within the same table.
- Must match the task requirement (expected keys/structure/types).
